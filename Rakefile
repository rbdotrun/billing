require('bundler/setup')

APP_RAKEFILE = File.expand_path("spec/dummy/Rakefile", __dir__)

load('rails/tasks/engine.rake')
load('rails/tasks/statistics.rake')
load('lib/tasks/load_dev.rake')

require('bundler/gem_tasks')